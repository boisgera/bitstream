sudo: required
dist: trusty
language: python
python:
  - 2.7

before_install:
  - sudo apt-get update 
  - sudo apt-get install -y python-pip python-numpy cython

install: 
  - sudo python setup.py --cython install

script: 
  - ls
  - pwd
  - cd /tmp && python -c "import bitstream"
  - ls
  - cd /tmp && python -c "from bitstream import BitStream"
#  - mkdir test && cd test && python -m doctest ../manual.txt

